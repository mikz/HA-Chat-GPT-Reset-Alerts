[project]
name = "ha-ai-usage"
version = "0.1.2"
requires-python = ">=3.13"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "custom_components"]
addopts = "--testmon"

[tool.coverage.run]
core = "ctrace"
